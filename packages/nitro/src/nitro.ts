import { INitro } from './types/nitro'

/**
 * 创建nitro
 * 使用对象+函数代替类的形式，好处是清晰，坏处是不能重写（不过可以通过适量的配置和钩子等解决）
 */
export function createNitro() {
    // -- nitro实例 --
    const nitro: INitro = {}

    // TODO

    return nitro
}
